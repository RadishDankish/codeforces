#include <bits/stdc++.h>

int main()
{
    int n{};
    int k{};

    std::cin>>n;
    std::cin>>k;

    std::vector<int> arr(n);

    for(int i{};i<n;i++)
    {
        std::cin>>arr[i];
    }

    


}